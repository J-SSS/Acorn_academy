Q1. case01 패키지를 생성해서 아래 내용을 수행하세요.
	1) Template 클래스(추상 클래스)
	- 필드: Scanner, cnt
	- 추상메소드: input(Object[] o), output(Score[] s), modify(매개변수), delete(매개변수)
	- Object 클래스 타입 활용 (형 변환)
	
	2) Score 클래스
	- 필드: 이름, 국어, 영어, 수학, 총점, 평균
	
	3) ScoreMain 클래스
	- 메뉴: 입력, 출력, 수정, 삭제
	- Score[] s = new Score[5];
	
	4) ScoreService 클래스
	- Template 클래스를 상속받아 구현
	- 수정/삭제는 이름을 입력받아 수행
	
Q2. case02 패키지를 생성해서 아래 내용을 수행하세요.
	1) Score 클래스
	- 필드: 이름, 학과, 점수  /  get/set
	2) ScoreService 클래스
	- 입력, 출력, 수정, 삭제
	3) Professor 클래스
	- 필드: 학과, 교수명  /  get/set
	4) ProfessorService 클래스
	- 입력, 출력, 수정, 삭제
	5) Main 클래스 
	- Score[] s = new Score[5]
	- Professor[] p = new Professor[5]
	- 메뉴1: 1.입력 2.출력 3.수정 4.삭제
	- 메뉴2: 1.성적 입력/출력/수정/삭제  2.교수진 입력/출력/수정/삭제
	6) MainService
	- 각 메소드별로 각 서비스(Score/Professor)로 분개
	
Q3. case03 패키지를 생성해서 아래 내용을 수행하세요.
	1) Template 클래스(추상 클래스)
	- 필드: Scanner, cnt
	- 추상메소드: input(Object[] o), output(Object[] o), modify(Object[] o), delete(Object[] o)
	2) Fruit 클래스 생성
	- 필드: 과일명, 가격  /  get/set
	3) FruitMain 클래스 생성
	  - 메뉴: 1.과일입력, 2.출력, 3.수정, 4.삭제
	  - Fruit[] s = new Fruit[5];
	4) FruitService 클래스 생성
	  - Template 클래스 상속받아 구현
	  - 수정/삭제는 과일명을 입력받아 수행
	
Q4. case04 패키지를 생성해서 아래 내용을 수행하세요.

	1. 시나리오
	- Player가 있고, Player는 GameLevel 속성을 가집니다.
	- 각 GameLevel 단계마다 run(), jump(), turn() 세 가지 기능이 업그레이드 된다.
	> 초보자 레벨: 천천히 달립니다. run()만 가능
  	> 중급자 레벨: 빠르게 달리고, 점프할 수 있습니다. run(), jump() 가능  
  	> 고급자 레벨: 엄청 빠르게 달리고, 높게 점프하고, 턴할 수 있습니다. run(), jump(), turn() 가능
	- Player는 한번에 하나의 레벨 상태만 가질 수 있습니다.
	- Player가 play(int count) 중에 레벨이 있는 go(int count)라는 메소드를 호출하면,
	  run()하고 count횟수만큼 jump()하고 turn()합니다.
	
	2. 클래스 생성
	1) Player 클래스
	  - 필드: level
	  - 메소드: getLevel(), upgradeLevel(PlayerLevel), play()
	2) PlayerLevel 클래스(추상 클래스)
	  - 추상 메소드: run(), jump(), turn(), showLevelMessage()
	  - 템플릿 메소드: go(int)
	3) BeginnerLevel, AdvancedLevel, SuperLevel 구현 클래스 
	4) MainBoardPlay 클래스(실행용)
	
	3. 기타
	- Player와 PlayerLevel은 포함 관계(Has-A)이다.
	- PlayerLevel과 3개의 구현 클래스들은 상속 관계(Is-A)이다.
	- 템플릿 메소드를 사용하여 출력 내용과 같이 실행 흐름 구현
	
	4. 출력 내용은 아래와 같다.
	****** 초급자 레벨입니다. ******
	천천히 달립니다.
	jump 못하지롱
	turn 못하지롱
	****** 중급자 레벨입니다. ******
	빨리 달립니다.
	높이 jump합니다.
	높이 jump합니다.
	turn 못하지롱
	****** 고급자 레벨입니다. ******
	엄청 빠르게 달립니다.
	아주 높이 jump합니다.
	아주 높이 jump합니다.
	아주 높이 jump합니다.
	turn 합니다.
	